/**
 * Production shell records are backend-owned.  This module intentionally has
 * no seed arrays; test/demo fixtures must live beside the tests or explicit
 * demo entry points and must never initialize the normal application shell.
 */
export {};
